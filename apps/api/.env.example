# docker-compose の postgres に接続する場合（ホストからは 5433）
DATABASE_URL="postgres://user:password@localhost:5433/memo"

# Hasura Actions の共有シークレット（#26）。リポジトリ直下の .env と同じ値にする。
# 未設定だと Actions のハンドラは全リクエストを 401 で拒否する（設定漏れで素通りさせないため）。
HASURA_ACTION_SECRET=""
